# data preparation

```rs
pub struct A {
    pub value: String,
}
```

# struct normal

- `pub` access modifier

```rs
pub struct B {
    pub a: A,
}
```

# struct tuple

- tuple element
- `pub` access modifier

```rs
pub struct B(pub A); // pub  结构体访问
```
